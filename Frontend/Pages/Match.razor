@page "/match/{Id}"
@inject HttpClient Http

<h2>Match!</h2>

@*
TODO: lägga in fält/tabell som visar info från match från backend*@

@code {
    [Parameter] public string Id { get; set; } = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        var response = Http.GetAsync($"/api/match/{Id}");
        Console.WriteLine("funkar!");
        
        //TODO - plocka ut info från response och sätt till variabler i html

    }
}