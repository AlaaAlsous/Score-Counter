<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Poängräknaren</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
                <NavLink class="nav-link" href="/">
                    <span class="bi bi-house-door-fill" aria-hidden="true"></span> Startsida
                </NavLink>
        </div>
        <div class="nav-item px-3">
                <NavLink class="nav-link" href="/create-game">
                    <span class="bi bi-plus-square-fill" aria-hidden="true"></span> Skapa match
                </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/match-enter">
                <span class="bi bi-dice-5" aria-hidden="true"></span> Match
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
